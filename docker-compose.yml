version: '3.8'

services:
    app:
        image: course-recommendation-frontend
        build: .
        ports:
            - 3000:3000
        volumes:
            - .:/usr/src/app
        command: npm run dev
